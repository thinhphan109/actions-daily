name: Nhật Ký Tự Động Hóa Hàng Ngày

on:
  # schedule:
    # Chạy vào 00:05 sáng giờ Việt Nam (tức 17:05 UTC ngày hôm trước)
    # - cron: '5 17 * * *'
    # Chạy vào 12:05 trưa giờ Việt Nam (tức 05:05 UTC cùng ngày)
    # - cron: '5 5 * * *'
  workflow_dispatch: # Chỉ cho phép chạy thủ công

permissions:
  contents: write

concurrency:
  group: daily-note
  cancel-in-progress: true

jobs:
  create-daily-note:
    runs-on: ubuntu-latest
    env:
      CITY: "Ho Chi Minh"
      LAT: "10.82"
      LON: "106.63"
      TZ: Asia/Ho_Chi_Minh

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 selenium

      - name: Install Chrome and ChromeDriver
        run: |
          # Cài đặt Chrome
          wget -q -O - https://dl.google.com/linux/linux_signing_key.pub | sudo apt-key add -
          echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" | sudo tee /etc/apt/sources.list.d/google-chrome.list
          sudo apt-get update
          sudo apt-get install -y google-chrome-stable
          
          # ChromeDriver sẽ được tự động quản lý bởi Selenium 4.x
          echo "Chrome version: $(google-chrome --version)"

      - name: Cập nhật nội dung tự động
        id: update_file
        shell: bash
        run: |
          # Phần còn lại của script bash giữ nguyên y như cũ
          set -euo pipefail
          
          today=$(date +%F)
          current_hour=$(date +%H)
          
          # Xác định thời điểm chạy và tạo tên file phù hợp
          if [ "$current_hour" -ge 0 ] && [ "$current_hour" -lt 12 ]; then
            time_suffix="12AM"
            display_time="00:05 sáng"
          else
            time_suffix="12PM" 
            display_time="12:05 trưa"
          fi
          
          output_dir="DailyNotes"
          mkdir -p "$output_dir"
          file="$output_dir/$today-$time_suffix.md"
          
          echo "📝 Tạo file nhật ký: $file (chạy lúc $display_time)"

          if ! command -v jq >/dev/null 2>&1 || ! command -v bc >/dev/null 2>&1; then
            echo "Installing missing tools (jq, bc)..."
            sudo apt-get update -y && sudo apt-get install -y jq bc
          fi

          function append_header() {
            if [ ! -f "$file" ]; then
              printf '# 🚀 Nhật Ký Tự Động Hóa - %s (%s)\n' "$today" "$display_time" > "$file"
              printf '\n*File được tạo tự động lúc %s*\n' "$display_time" >> "$file"
            fi
          }
          function append_calendar() {
            if ! grep -q '' "$file"; then
              dow_num=$(date +%u); case "$dow_num" in 1) dow="Thứ Hai" ;; 2) dow="Thứ Ba" ;; 3) dow="Thứ Tư" ;; 4) dow="Thứ Năm" ;; 5) dow="Thứ Sáu" ;; 6) dow="Thứ Bảy" ;; 7) dow="Chủ Nhật" ;; esac
              iso_week=$(date +%V); day_of_year=$(date +%j)
              { echo; echo "---"; echo ""; echo "## 🗓️ Lịch Hôm Nay"; echo "- **Ngày**: $today ($dow)"; echo "- **Tuần (ISO)**: $iso_week / **Ngày trong năm**: $day_of_year"; } >> "$file"
            fi
          }
          function append_holiday_check() {
            if ! grep -q '' "$file"; then
              status_code=$(curl -o /dev/null -s -w "%{http_code}" "https://date.nager.at/api/v3/IsTodayPublicHoliday/VN" || true)
              holiday_info=""
              if [ "$status_code" -eq 200 ]; then
                holiday_info="- **Thông báo**: 🎉 Hôm nay là một ngày nghỉ lễ tại Việt Nam."
              elif [ "$status_code" -eq 204 ]; then
                holiday_info="- **Thông báo**: Hôm nay không phải là ngày nghỉ lễ."
              fi
              if [ -n "$holiday_info" ]; then
                { echo; echo ""; echo "## 🇻🇳 Ngày Lễ Việt Nam"; echo "$holiday_info"; } >> "$file"
              fi
            fi
          }
          function append_weather_and_uv() {
            if ! grep -q '' "$file"; then
              wjson=$(curl -fsSL "https://api.open-meteo.com/v1/forecast?latitude=${LAT}&longitude=${LON}&current_weather=true&daily=uv_index_max&timezone=Asia%2FHo_Chi_Minh" || true)
              if [ -n "$wjson" ] && echo "$wjson" | jq -e . >/dev/null 2>&1; then
                temp=$(echo "$wjson" | jq -r '.current_weather.temperature // empty'); wind=$(echo "$wjson" | jq -r '.current_weather.windspeed // empty');
                uv_index=$(echo "$wjson" | jq -r '.daily.uv_index_max[0] // empty' | cut -d'.' -f1)
                uv_desc="Không xác định"
                if [ -n "$uv_index" ]; then
                    if (( uv_index <= 2 )); then uv_desc="🟢 Thấp - Không cần bảo vệ"
                    elif (( uv_index <= 5 )); then uv_desc="🟡 Trung bình - Che nắng"
                    elif (( uv_index <= 7 )); then uv_desc="🟠 Cao - Cần bôi kem chống nắng"
                    elif (( uv_index <= 10 )); then uv_desc="🔴 Rất cao - Hạn chế ra ngoài"
                    else uv_desc="🟣 Cực kỳ cao - Tránh ra ngoài trời"
                    fi
                fi
                { echo; echo "---"; echo ""; echo "## 🌤️ Thời tiết & Chỉ số UV tại ${CITY}"; echo "- **Nhiệt độ**: ${temp}°C / **Gió**: ${wind} km/h"; if [ -n "$uv_index" ]; then echo "- **Chỉ số UV**: $uv_index ($uv_desc)"; fi; } >> "$file"
              fi
            fi
          }
          function append_financial_data() {
            if ! grep -q '<!-- FINANCE-MODULE -->' "$file"; then
              ratejson=$(curl --connect-timeout 5 -fsSL "https://open.er-api.com/v6/latest/USD" || true)
              usd_vnd_rate="Đang cập nhật..."
              if [ -n "$ratejson" ] && echo "$ratejson" | jq -e . >/dev/null 2>&1; then 
                rate_raw=$(echo "$ratejson" | jq -r '.rates.VND')
                if [ -n "$rate_raw" ] && [ "$rate_raw" != "null" ]; then 
                  usd_vnd_rate=$(printf "%.0f" "$rate_raw")
                fi
              fi
              { echo; echo "---"; echo "<!-- FINANCE-MODULE -->"; echo "## 💰 Tỷ Giá Ngoại Tệ"; echo "- **Tỷ giá USD/VND**: 1 USD ≈ $usd_vnd_rate VND"; echo; echo "*Giá vàng chi tiết xem phần PNJ bên dưới*"; } >> "$file"
            fi
          }
          function append_pnj_gold() {
            if ! grep -q '<!-- PNJ-GOLD-MODULE -->' "$file"; then
              echo "🔍 Đang crawl giá vàng từ PNJ bằng Selenium..."
              local pnj_output_file="pnj_gold_prices.md"
              
              # Đặt biến môi trường để Selenium hoạt động trong headless mode
              export DISPLAY=:99
              
              # Chạy script crawl giá vàng với timeout
              timeout 60s python get_gold_price.py "$pnj_output_file" || {
                echo "⚠️ Timeout hoặc lỗi khi crawl giá vàng PNJ"
                echo "# Bảng Giá Vàng PNJ" > "$pnj_output_file"
                echo "*Không thể crawl dữ liệu vào lúc này do timeout hoặc lỗi kỹ thuật*" >> "$pnj_output_file"
              }
              
              if [ -s "$pnj_output_file" ]; then
                { echo; echo "---"; echo "<!-- PNJ-GOLD-MODULE -->"; echo "## ⚜️ Bảng Giá Vàng PNJ Chi Tiết"; echo; cat "$pnj_output_file"; } >> "$file"
                echo "✅ Đã thêm bảng giá vàng PNJ vào nhật ký"
              else
                { echo; echo "---"; echo "<!-- PNJ-GOLD-MODULE -->"; echo "## ⚜️ Bảng Giá Vàng PNJ Chi Tiết"; echo "❌ Không thể crawl dữ liệu giá vàng PNJ vào lúc này."; echo "Vui lòng thử lại sau hoặc kiểm tra [trực tiếp tại PNJ](https://www.pnj.com.vn/site/gia-vang)"; } >> "$file"
                echo "⚠️ Không thể crawl giá vàng, đã thêm thông báo lỗi"
              fi
              
              # Dọn dẹp file tạm
              [ -f "$pnj_output_file" ] && rm "$pnj_output_file"
            fi
          }
          function append_crypto_prices() { if ! grep -q '' "$file"; then COIN_IDS="bitcoin,ethereum,solana,binancecoin,ripple,dogecoin,tether"; cryptojson=$(curl -fsSL "https://api.coingecko.com/api/v3/simple/price?ids=${COIN_IDS}&vs_currencies=usd" || true); if [ -n "$cryptojson" ] && echo "$cryptojson" | jq -e . >/dev/null 2>&1; then output=""; btc_price_raw=$(echo "$cryptojson" | jq -r '.bitcoin.usd'); if [ -n "$btc_price_raw" ] && [ "$btc_price_raw" != "null" ]; then btc_price=$(printf "%.2f" "$btc_price_raw"); output+="- **Bitcoin (BTC)**: \$${btc_price} USD\n"; fi; eth_price_raw=$(echo "$cryptojson" | jq -r '.ethereum.usd'); if [ -n "$eth_price_raw" ] && [ "$eth_price_raw" != "null" ]; then eth_price=$(printf "%.2f" "$eth_price_raw"); output+="- **Ethereum (ETH)**: \$${eth_price} USD\n"; fi; sol_price_raw=$(echo "$cryptojson" | jq -r '.solana.usd'); if [ -n "$sol_price_raw" ] && [ "$sol_price_raw" != "null" ]; then sol_price=$(printf "%.2f" "$sol_price_raw"); output+="- **Solana (SOL)**: \$${sol_price} USD\n"; fi; bnb_price_raw=$(echo "$cryptojson" | jq -r '.binancecoin.usd'); if [ -n "$bnb_price_raw" ] && [ "$bnb_price_raw" != "null" ]; then bnb_price=$(printf "%.2f" "$bnb_price_raw"); output+="- **BNB**: \$${bnb_price} USD\n"; fi; xrp_price_raw=$(echo "$cryptojson" | jq -r '.ripple.usd'); if [ -n "$xrp_price_raw" ] && [ "$xrp_price_raw" != "null" ]; then xrp_price=$(printf "%.4f" "$xrp_price_raw"); output+="- **XRP**: \$${xrp_price} USD\n"; fi; doge_price_raw=$(echo "$cryptojson" | jq -r '.dogecoin.usd'); if [ -n "$doge_price_raw" ] && [ "$doge_price_raw" != "null" ]; then doge_price=$(printf "%.4f" "$doge_price_raw"); output+="- **Dogecoin (DOGE)**: \$${doge_price} USD\n"; fi; usdt_price_raw=$(echo "$cryptojson" | jq -r '.tether.usd'); if [ -n "$usdt_price_raw" ] && [ "$usdt_price_raw" != "null" ]; then usdt_price=$(printf "%.4f" "$usdt_price_raw"); output+="- **Tether (USDT)**: \$${usdt_price} USD\n"; fi; output=$(echo -e "${output}" | sed '/^$/d'); { echo; echo "---"; echo ""; echo "## 📈 Giá Tiền Mã Hóa"; echo -e "$output"; } >> "$file"; fi; fi; }
          function append_iss_location() { if ! grep -q '' "$file"; then issjson=$(curl -fsSL "http://api.open-notify.org/iss-now.json" || true); if [ -n "$issjson" ] && echo "$issjson" | jq -e . >/dev/null 2>&1; then lat=$(echo "$issjson" | jq -r '.iss_position.latitude'); lon=$(echo "$issjson" | jq -r '.iss_position.longitude'); geo_json=$(curl -fsSL "https://api.bigdatacloud.net/data/reverse-geocode-client?latitude=${lat}&longitude=${lon}&localityLanguage=en" || true); location="Trên đại dương hoặc khu vực không xác định"; if [ -n "$geo_json" ] && echo "$geo_json" | jq -e . >/dev/null 2>&1; then country=$(echo "$geo_json" | jq -r '.countryName // empty'); city=$(echo "$geo_json" | jq -r '.city // empty'); principalSubdivision=$(echo "$geo_json" | jq -r '.principalSubdivision // empty'); if [ -n "$country" ]; then location="$country"; if [ -n "$principalSubdivision" ]; then location="$principalSubdivision, $location"; fi; if [ -n "$city" ]; then location="$city, $location"; fi; fi; fi; { echo; echo "---"; echo ""; echo "## 🛰️ Vị Trí Trạm ISS"; echo "- **Vị trí hiện tại**: $location"; echo "- **Tọa độ**: $lat, $lon"; } >> "$file"; fi; fi; }
          function append_rocket_launch() { if ! grep -q '' "$file"; then launchjson=$(curl -fsSL "https://ll.thespacedevs.com/2.2.0/launch/upcoming/?limit=1" || true); if [ -n "$launchjson" ] && echo "$launchjson" | jq -e '.results | length > 0' >/dev/null 2>&1; then launch=$(echo "$launchjson" | jq '.results[0]'); name=$(echo "$launch" | jq -r '.name'); mission=$(echo "$launch" | jq -r '.mission.name'); iso_date=$(echo "$launch" | jq -r '.window_start'); launch_time=$(date -d "$iso_date" +"%d/%m/%Y, %H:%M (Giờ Việt Nam)"); { echo; echo "---"; echo ""; echo "## 🚀 Lịch Phóng Tên Lửa"; echo "- **Tên lửa**: $name"; echo "- **Nhiệm vụ**: $mission"; echo "- **Thời gian dự kiến**: $launch_time"; } >> "$file"; fi; fi; }
          function append_advice() { if ! grep -q '' "$file"; then advice_en_json=$(curl -fsSL "https://api.adviceslip.com/advice" || true); if [ -n "$advice_en_json" ] && echo "$advice_en_json" | jq -e . >/dev/null 2>&1; then advice_en=$(echo "$advice_en_json" | jq -r '.slip.advice'); advice_vi="$advice_en"; advice_en_encoded=$(echo "$advice_en" | sed 's/ /%20/g'); translation_json=$(curl -fsSL "https://api.mymemory.translated.net/get?q=${advice_en_encoded}&langpair=en|vi" || true); if [ -n "$translation_json" ] && echo "$translation_json" | jq -e . >/dev/null 2>&1; then translated_text=$(echo "$translation_json" | jq -r '.responseData.translatedText'); if [ -n "$translated_text" ] && [ "$translated_text" != "NO QUERY SPECIFIED!" ] && [ "$translated_text" != "$advice_en" ]; then advice_vi="$translated_text"; fi; fi; { echo; echo "---"; echo ""; echo "## 💡 Lời khuyên ngắn"; echo "> $advice_vi"; } >> "$file"; fi; fi; }
          function append_numbers_fact() { if ! grep -q '' "$file"; then numjson_en=$(curl -fsSL "http://numbersapi.com/random/trivia?json" || true); if [ -n "$numjson_en" ] && echo "$numjson_en" | jq -e . >/dev/null 2>&1; then fact_en=$(echo "$numjson_en" | jq -r '.text'); fact_vi="$fact_en"; fact_en_encoded=$(echo "$fact_en" | sed 's/ /%20/g'); translation_json=$(curl -fsSL "https://api.mymemory.translated.net/get?q=${fact_en_encoded}&langpair=en|vi" || true); if [ -n "$translation_json" ] && echo "$translation_json" | jq -e . >/dev/null 2>&1; then translated_text=$(echo "$translation_json" | jq -r '.responseData.translatedText'); if [ -n "$translated_text" ] && [ "$translated_text" != "NO QUERY SPECIFIED!" ] && [ "$translated_text" != "$fact_en" ]; then fact_vi="$translated_text"; fi; fi; { echo; echo "---"; echo ""; echo "## #️⃣ Sự Thật Về Con Số"; echo "> $fact_vi"; } >> "$file"; fi; fi; }
          function append_stoic_quote() { if ! grep -q '' "$file"; then stoicjson=$(curl -fsSL "https://stoic-quotes.com/api/quote" || true); if [ -n "$stoicjson" ] && echo "$stoicjson" | jq -e . >/dev/null 2>&1; then quote_en=$(echo "$stoicjson" | jq -r '.quote'); author=$(echo "$stoicjson" | jq -r '.author'); if [ -n "$quote_en" ] && [ "$quote_en" != "null" ]; then quote_vi="$quote_en"; quote_en_encoded=$(echo "$quote_en" | sed 's/ /%20/g'); translation_json=$(curl -fsSL "https://api.mymemory.translated.net/get?q=${quote_en_encoded}&langpair=en|vi" || true); if [ -n "$translation_json" ] && echo "$translation_json" | jq -e . >/dev/null 2>&1; then translated_text=$(echo "$translation_json" | jq -r '.responseData.translatedText'); if [ -n "$translated_text" ] && [ "$translated_text" != "NO QUERY SPECIFIED!" ] && [ "$translated_text" != "$quote_en" ]; then quote_vi="$translated_text"; fi; fi; { echo; echo "---"; echo ""; echo "## 🏛️ Triết Lý Stoic"; echo "> $quote_vi"; echo ">"; echo "> — ***$author***"; } >> "$file"; fi; fi; fi; }
          function append_footer() {
            if ! grep -q '' "$file"; then
              { echo; echo "---"; echo ""; echo "> *Ghi chú này được tạo tự động bởi [GitHub Actions](https://github.com/features/actions).*" ; } >> "$file"
            fi
          }

          echo "Bắt đầu cập nhật file: $file"
          append_header
          append_calendar
          append_holiday_check
          append_weather_and_uv
          append_financial_data
          append_pnj_gold
          append_crypto_prices
          append_iss_location
          append_rocket_launch
          append_numbers_fact
          append_advice
          append_stoic_quote
          append_footer
          echo "Hoàn thành cập nhật nội dung."
          
          git add "$file"
          
          echo "Đang tạo tóm tắt cho Discord..."
          wjson_discord=$(curl -fsSL "https://api.open-meteo.com/v1/forecast?latitude=${LAT}&longitude=${LON}&current_weather=true&daily=uv_index_max&timezone=Asia%2FHo_Chi_Minh" || true)
          weather_line="Thời tiết: Đang cập nhật..."; uv_line="Chỉ số UV: Đang cập nhật..."
          if [ -n "$wjson_discord" ] && echo "$wjson_discord" | jq -e . >/dev/null 2>&1; then temp=$(echo "$wjson_discord" | jq -r '.current_weather.temperature // empty'); wind=$(echo "$wjson_discord" | jq -r '.current_weather.windspeed // empty'); weather_line="Nhiệt độ: ${temp}°C, gió ${wind} km/h"; uv_index=$(echo "$wjson_discord" | jq -r '.daily.uv_index_max[0] // empty' | cut -d'.' -f1); if [ -n "$uv_index" ]; then uv_desc=""; if (( uv_index <= 2 )); then uv_desc="Thấp"; elif (( uv_index <= 5 )); then uv_desc="Trung bình"; elif (( uv_index <= 7 )); then uv_desc="Cao"; elif (( uv_index <= 10 )); then uv_desc="Rất cao"; else uv_desc="Cực kỳ cao"; fi; uv_line="Cao nhất: $uv_index ($uv_desc)"; fi; fi
          advice_discord_json=$(curl -fsSL "https://api.adviceslip.com/advice" || true)
          advice_line="Hôm nay là một ngày tuyệt vời!"
          if [ -n "$advice_discord_json" ] && echo "$advice_discord_json" | jq -e . >/dev/null 2>&1; then advice_en=$(echo "$advice_discord_json" | jq -r '.slip.advice'); advice_vi="$advice_en"; advice_en_encoded=$(echo "$advice_en" | sed 's/ /%20/g'); translation_json=$(curl -fsSL "https://api.mymemory.translated.net/get?q=${advice_en_encoded}&langpair=en|vi" || true); if [ -n "$translation_json" ] && echo "$translation_json" | jq -e . >/dev/null 2>&1; then translated_text=$(echo "$translation_json" | jq -r '.responseData.translatedText'); if [ -n "$translated_text" ] && [ "$translated_text" != "NO QUERY SPECIFIED!" ] && [ "$translated_text" != "$advice_en" ]; then advice_vi="$translated_text"; fi; fi; advice_line="$advice_vi"; fi
          holiday_discord_status=$(curl -o /dev/null -s -w "%{http_code}" "https://date.nager.at/api/v3/IsTodayPublicHoliday/VN" || true)
          holiday_line=""; if [ "$holiday_discord_status" -eq 200 ]; then holiday_line="🎉 Chúc mừng! Hôm nay là ngày nghỉ lễ."; fi
          echo "weather_field_value=$weather_line" >> $GITHUB_OUTPUT; echo "uv_field_value=$uv_line" >> $GITHUB_OUTPUT; echo "advice_field_value=$advice_line" >> $GITHUB_OUTPUT; echo "holiday_field_value=$holiday_line" >> $GITHUB_OUTPUT

      - name: Commit và đẩy code lên repo (nếu có thay đổi)
        id: commit_step
        shell: bash
        run: |
          set -euo pipefail
          if ! git diff --cached --quiet; then
            echo "changes=true" >> $GITHUB_OUTPUT; git config user.name "${{ github.actor }}"; git config user.email "${{ github.actor }}@users.noreply.github.com"; git commit -m "📝 Cập nhật nhật ký tự động ngày $(date +%F)"; git push
          else
            echo "changes=false" >> $GITHUB_OUTPUT; echo "Không có gì thay đổi để commit."
          fi

      - name: Gửi thông báo đến Discord
        if: steps.commit_step.outputs.changes == 'true'
        shell: bash
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          WEATHER_DATA: ${{ steps.update_file.outputs.weather_field_value }}
          UV_DATA: ${{ steps.update_file.outputs.uv_field_value }}
          ADVICE_DATA: ${{ steps.update_file.outputs.advice_field_value }}
          HOLIDAY_DATA: ${{ steps.update_file.outputs.holiday_field_value }}
        run: |
          if [ -z "$DISCORD_WEBHOOK_URL" ]; then echo "Cảnh báo: DISCORD_WEBHOOK_URL secret chưa được thiết lập. Bỏ qua việc gửi thông báo."; exit 0; fi
          
          # Xác định tên file cho Discord notification
          today_discord=$(date +%F)
          current_hour_discord=$(date +%H)
          if [ "$current_hour_discord" -ge 0 ] && [ "$current_hour_discord" -lt 12 ]; then
            time_suffix_discord="12AM"
          else
            time_suffix_discord="12PM"
          fi
          
          file_url="https://github.com/${{ github.repository }}/blob/${{ github.ref_name }}/DailyNotes/$today_discord-$time_suffix_discord.md"
          # Tạo title với thời gian
          if [ "$current_hour_discord" -ge 0 ] && [ "$current_hour_discord" -lt 12 ]; then
            time_display="🌅 Sáng"
          else
            time_display="🌇 Trưa"
          fi
          
          description="File nhật ký đã được cập nhật với các thông tin mới nhất. [Nhấn vào đây để xem chi tiết]($file_url)"
          json_payload=$(jq -n --arg title "📝 Nhật Ký $time_display: $(date +'%d/%m/%Y')" --arg url "$file_url" --arg description "$description" --arg weather_value "$WEATHER_DATA" --arg uv_value "$UV_DATA" --arg advice_value "$ADVICE_DATA" --arg holiday_value "$HOLIDAY_DATA" '{"username": "Nhật Ký Bot","avatar_url": "https://github.githubassets.com/images/modules/logos_page/GitHub-Mark.png","embeds": [{"title": $title,"url": $url,"description": $description,"color": 3447003,"fields": [{ "name": "🌤️ Thời tiết (${{ env.CITY }})", "value": $weather_value, "inline": true },{ "name": "☀️ Chỉ số UV", "value": $uv_value, "inline": true },{ "name": "💡 Lời khuyên ngắn", "value": $advice_value, "inline": false }],"footer": { "text": "Cập nhật lúc" },"timestamp": "'$(date -u +"%Y-%m-%dT%H:%M:%S.000Z")'"}]} | .embeds[0].fields += (if $holiday_value != "" then [{ "name": "🇻🇳 Ngày Lễ Việt Nam", "value": $holiday_value, "inline": false }] else [] end)')
          curl -H "Content-Type: application/json" -d "$json_payload" "$DISCORD_WEBHOOK_URL"
